version: '3'
services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - .:/var/www/html
    ports:
      - "8000:80"
    networks:
      - risk-v1-network

networks:
  risk-v1-network:
    driver: bridge